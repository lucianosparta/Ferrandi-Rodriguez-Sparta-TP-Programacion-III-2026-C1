Agregar archivo .env en el root -> PORT = 3000

Una vez clonado el proyecto y habiendo traido los ultimos cambios (git pull):

abrir terminal en el root:

Para instalar/reinstalar dependencias: "npm install" ->  lee el package.json y reinstala automáticamente(express, sequilazer, mysql2)

Para levantar el servidor: "npm start"